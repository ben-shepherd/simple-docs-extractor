{
  "name": "@benjamins94/simple-docs-extractor",
  "type": "module",
  "version": "1.0.2",
  "description": "A simple tool to extract documentation comments from codebases",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ben-shepherd/simple-docs-extractor.git"
  },
  "license": "ISC",
  "author": "ben.shepherd@gmx.com",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "prepare": "npm run build",
    "test": "jest --passWithNoTests --runInBand",
    "test:watch": "jest --watch --passWithNoTests --runInBand",
    "test:coverage": "jest --coverage --passWithNoTests",
    "build": "tsc --skipLibCheck && npx tsc-alias && npm run copy-templates",
    "copy-templates": "copyfiles -f src/templates/*.md dist/templates",
    "build:watch": "tsc --watch --skipLibCheck",
    "lint": "eslint --ignore-pattern .gitignore \"{src,tests}/**/*.+(ts|js|tsx)\"",
    "lint:fix": "eslint --fix --ignore-pattern .gitignore \"{src,tests}/**/*.+(ts|js|tsx)\"",
    "typecheck": "tsc --noEmit",
    "format": "prettier --ignore-path .prettierignore --write \"**/*.+(js|ts|json|tsx|mdx)\" --log-level silent",
    "format:check": "prettier --ignore-path .prettierignore --check \"**/*.+(js|ts|json|tsx|mdx)\" --log-level silent",
    "check-exports": "node ./scripts/check-exports.js",
    "check": "npm run format && npm run lint:fix && npm run typecheck && npm run check-exports",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "depcheck": "depcheck",
    "tslint": "tslint -p tsconfig.json",
    "docs:publish": "npm run build && node ./dist/scripts/trigger-publish-docs.js"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.3",
    "copyfiles": "^2.4.1",
    "jest": "^30.1.3",
    "jest-environment-jsdom": "^30.1.2",
    "prettier": "^3.6.2",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.16",
    "tslint": "^5.20.1"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "glob": "^11.0.3"
  }
}
